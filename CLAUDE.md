@AGENTS.md

## 设计相关流程

每次涉及设计相关内容（UI/UX、页面布局、交互设计、视觉方案等）时，必须先调用 `awesome-design-md` skill 获取设计参考和灵感，再进行实现。

## 错误修复流程

每次遇到错误需要修复时，必须先完成以下步骤再动手写代码：

1. **分析错误** — 向用户说明这是什么类型的错误（编译错误、运行时错误、逻辑错误等），以及错误的根本原因
2. **总结方案** — 说明计划如何修复，以及修复过程中是否有优化空间
3. **执行操作** — 用户确认后再进行实际的代码修改
